# Waitlist lead forwarding (optional). If both are set, every waitlist submission is
# pushed to your Telegram chat. Leave unset to just log submissions to the server console.
# Create the bot via @BotFather, then DM it /start and read your chat id (e.g. via @userinfobot).
TG_BOT_TOKEN=
TG_CHAT_ID=
